# 🔒 Настройка работы через SOCKS5 прокси

## Когда это нужно?

Если Grafana и SSH-сервер Gatling доступны только через промежуточную VM (jump host), используйте эту инструкцию.

---

## ⚙️ Быстрая настройка

### 1. Запустите SSH туннель на вашем Mac

```bash
# В отдельном терминале или tmux
ssh -N -D 1081 lex@45.132.75.118
```

**Параметры:**
- `-N` - не выполнять команды, только туннель
- `-D 1081` - создать SOCKS5 прокси на локальном порту 1081
- `lex@45.132.75.118` - ваша VM (jump host)

**Проверка:**
```bash
# Проверьте, что порт слушается
lsof -i :1081
# или
netstat -an | grep 1081
```

---

### 2. Настройте `.env`

Добавьте в `.env` файл:

```bash
# ========== PROXY CONFIGURATION ==========
# Включить/выключить использование прокси
PROXY_ENABLED=true

# SOCKS5 прокси URL (используйте socks5h:// для DNS резолва на VM)
PROXY_URL=socks5h://127.0.0.1:1081

# Таймаут проверки прокси (секунды)
PROXY_CHECK_TIMEOUT=10

# SSH прокси настройки (для SSH/SCP через SOCKS5)
SSH_PROXY_HOST=127.0.0.1
SSH_PROXY_PORT=1081
```

**Важно:**
- Используйте `127.0.0.1` (localhost), а не IP VM
- Используйте схему `socks5h://` (с буквой `h`) для DNS резолва на стороне VM

---

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

Это установит `PySocks==1.7.1`, необходимый для SOCKS5 поддержки.

---

### 4. Проверьте настройки

Запустите тестовый скрипт:

```bash
python test_proxy.py
```

Этот скрипт проверит:
- ✅ Загрузку конфигурации прокси
- ✅ Доступность SOCKS5 прокси
- ✅ HTTP запросы через прокси
- ✅ SSH подключение через прокси

**После успешного теста можно удалить:**
```bash
rm test_proxy.py
```

---

### 5. Запустите основной скрипт

Теперь запускайте как обычно:

```bash
# Скачать только метрики Grafana
python -m src.main -grafana

# Скачать отчеты Gatling и метрики Grafana
python -m src.main -gatling -grafana
```

Все запросы будут автоматически идти через прокси.

---

## 🔄 Отключение прокси

Если нужно вернуться к прямому подключению:

```bash
# В .env установите
PROXY_ENABLED=false
```

Или просто закомментируйте:
```bash
# PROXY_ENABLED=true
```

---

## 🐛 Решение проблем

### Ошибка: "Прокси недоступен"

**Причины:**
1. SSH туннель не запущен или упал
2. Неверный порт/адрес прокси
3. Firewall блокирует подключение

**Решение:**
```bash
# Проверьте, что туннель работает
lsof -i :1081

# Перезапустите туннель
ssh -N -D 1081 lex@45.132.75.118

# В другом терминале проверьте
python test_proxy.py
```

---

### Ошибка: "Timeout" при скачивании

**Причина:** Прокси слишком медленный или перегружен

**Решение:**
```bash
# В .env увеличьте таймауты
PROXY_CHECK_TIMEOUT=30
```

---

### SSH туннель постоянно падает

**Решение:** Запустите туннель с keep-alive:

```bash
ssh -N -D 1081 lex@45.132.75.118 \
  -o ServerAliveInterval=60 \
  -o ServerAliveCountMax=3
```

Или используйте `autossh`:
```bash
autossh -M 0 -N -D 1081 lex@45.132.75.118 \
  -o ServerAliveInterval=60 \
  -o ServerAliveCountMax=3
```

---

## 📋 Архитектура

```
┌──────────────┐         SSH туннель          ┌──────────────┐
│   Ваш Mac    │  ─────────────────────────>  │      VM      │
│              │    -D 1081 (SOCKS5)          │ (Jump Host)  │
└──────────────┘                               └──────────────┘
       │                                              │
       │ 127.0.0.1:1081                              │
       │ (локальный прокси)                          │
       ▼                                              ▼
┌──────────────┐                               ┌──────────────┐
│Python скрипт │ ──── HTTP(S) через прокси ──> │   Grafana    │
│              │                               │   API        │
│              │ ──── SSH/SCP через прокси ──> │   SSH Host   │
└──────────────┘                               │  (Gatling)   │
       │                                       └──────────────┘
       │ Файлы сохраняются
       ▼ локально на Mac
┌──────────────┐
│  reports/    │
│  ├─ gatling/ │
│  └─ metrics/ │
└──────────────┘
```

**Ключевые моменты:**
1. SSH туннель создаёт SOCKS5 прокси на `127.0.0.1:1081` (на Mac)
2. Все HTTP(S) запросы к Grafana идут через этот прокси
3. SSH/SCP подключения к Gatling серверу тоже идут через прокси
4. DNS резолв происходит на VM (благодаря `socks5h://`)
5. Файлы скачиваются на Mac (не на VM)

---

## 🧹 Удаление тестового кода

После успешной настройки можно удалить:

```bash
rm test_proxy.py
rm PROXY_SETUP.md  # эта документация тоже опционально
```

Основная функциональность встроена в:
- `src/config.py` - загрузка прокси конфигурации
- `src/proxy_utils.py` - утилиты для работы с прокси
- `src/grafana_service.py` - HTTP через прокси
- `src/ssh_service.py` - SSH через прокси

---

## 📚 Дополнительная информация

### Двойной туннель (на VM)

Если на VM тоже запущен туннель:
```bash
ssh -N -g -D 0.0.0.0:1081 localhost \
  -o ExitOnForwardFailure=yes \
  -o ServerAliveInterval=60 \
  -o ServerAliveCountMax=3
```

**Это создаёт:**
```
Mac → SSH → VM → localhost
        └── 127.0.0.1:1081 на Mac
                └── 0.0.0.0:1081 на VM
```

Для скрипта на Mac используйте локальный прокси (`127.0.0.1:1081`), а не IP VM.

---

### Переменные окружения vs код

**Текущая реализация:** Прокси настраивается явно в коде через параметр `proxies`.

**Альтернатива:** Можно использовать переменные окружения:
```bash
export HTTPS_PROXY=socks5h://127.0.0.1:1081
export HTTP_PROXY=socks5h://127.0.0.1:1081
python -m src.main -grafana
```

Но явная настройка в коде даёт больше контроля и лучше для отладки.

---

## ✅ Чеклист готовности

- [ ] SSH туннель запущен (`ssh -N -D 1081 ...`)
- [ ] Порт 1081 слушается (`lsof -i :1081`)
- [ ] `.env` настроен с правильными параметрами
- [ ] `PySocks` установлен (`pip show PySocks`)
- [ ] Тест пройден (`python test_proxy.py`)
- [ ] Основной скрипт работает

---

Автор: AI Assistant
Дата: 2025-12-01

